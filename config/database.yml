
# PostgreSQL 
default: &default
  adapter: postgresql
  encoding: utf8
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: <%= ENV['POSTGRES_USER'] { 'haga' } %>
  password: <%= ENV['POSTGRES_PASSWORD'] { '1123haga' } %>
  host: <%= ENV['POSTGRES_HOST'] { 'http://localhost:5432' } %>
  database: <%= ENV['POSTGRES_DB'] { 'myapp_development' } %>

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
